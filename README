Installation:
    git clone git://github.om/pandaman2123/pandavim.git ~/.vim

Create symlinks:
    ln -s ~/.vim/vimrc ~/.vimrc
    ln -s ~/.vim/gvimrc ~/.gvimrc

switch to the ~/.vim directory and fetch submodules
    cd ~/.vim
    git submodule init
    git submodule update
